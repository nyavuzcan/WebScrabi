package com.openhere;

import org.jsoup.Jsoup;
import org.w3c.dom.Document;

public class info {

  //if the input is a string,

  /*String html ="<html>" +
      "<head>" +
      "<title>" +
      "js" ;
  Document doc = (Document) Jsoup.parse(html);*/

  //If ınput A file

  /*

  File file = new File("index.htm");
  Document doc = Jsoup.parse(file,"utf-8);

   */
/*

IF FROM URL
  Document doc = Jsoup.connect("ggogle.com").get();
 */
// LISTING ALL URLS WITHIN A HTML PAGE

  /*
  Elements link = doc.select("a[href]");
  for(Elemnt linkk : links){
  System.out.println("\nlink:"+link.attr("href));
  Sout(link.text());
  }
   */


}
